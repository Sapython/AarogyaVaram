// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  firebase: {
    projectId: 'aarogyavaram-hs',
    appId: '1:699731725550:web:d0685218d8cf2781e637ff',
    storageBucket: 'aarogyavaram-hs.appspot.com',
    apiKey: 'REPLACEMENT_STRING',
    authDomain: 'aarogyavaram-hs.firebaseapp.com',
    messagingSenderId: '699731725550',
    measurementId: 'G-STRWWMHBSG',
  },
  production: false,
  printerServerUrl: 'http://localhost:3000',
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
